const styles = (theme) => ({
    preloader: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: 'inherit',
        width: '100%',
        height: 100,
        padding: 20,
    }
})

export default styles;
